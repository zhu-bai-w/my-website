---
interface Props {
  label: string;
  tone?: 'default' | 'leaf';
}

const { label, tone = 'default' } = Astro.props;
---

<span class:list={['badge', tone === 'leaf' && 'badge-leaf']}>{label}</span>
